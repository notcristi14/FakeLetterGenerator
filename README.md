# FakeLetterGenerator
FakeLetterGenerator is a C# tool that swaps standard text/symbols (like {}, ==, a-z) for visually identical Unicode "ghost" characters. By replacing 100% of the input, it removes the digital footprint that checkers flag. It features a colorful UI, timestamped file naming for unique outputs, and automatic directory creation.
